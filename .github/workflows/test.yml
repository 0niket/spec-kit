name: Test
permissions:
  contents: read

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  python-tests:
    name: Python Tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12', '3.13']
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run pytest with coverage
        run: |
          pytest tests/python/ --cov=src/specify_cli --cov-report=xml --cov-report=term-missing

      - name: Upload coverage to Codecov
        if: matrix.python-version == '3.11'
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.xml
          fail_ci_if_error: false
        continue-on-error: true

  bash-tests:
    name: Bash Tests (bats)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          sudo apt-get update
          sudo apt-get install -y bats

      - name: Run bats tests
        run: |
          bats tests/bash/

  # PowerShell tests using Pester (placeholder for future implementation)
  # powershell-tests:
  #   name: PowerShell Tests (Pester)
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #
  #     - name: Install Pester
  #       shell: pwsh
  #       run: Install-Module -Name Pester -Force -Scope CurrentUser -MinimumVersion 5.0.0
  #
  #     - name: Run Pester tests
  #       shell: pwsh
  #       run: |
  #         Invoke-Pester -Path tests/powershell/ -Output Detailed -CI
